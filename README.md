# apollo-fetch-browserify
Shows how to use apollo-fetch in browser with browserify

## Getting started

` npm install -g browserify grunt `


Clone this repo and then run
1. ` npm install `
2. ` grunt serve `
